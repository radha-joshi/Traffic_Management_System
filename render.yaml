services:
  - type: web
    name: traffic-management-system
    env: docker
    plan: free
    region: singapore
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
      # PORT is injected automatically by Render
